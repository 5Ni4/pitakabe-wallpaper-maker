# ぴた壁 次期版の開発方針

## このコピーの位置付け

- このディレクトリは、公開済みの製品ver.1.0から復元した独立したGitリポジトリです。
- 復元元は `v1.0.0`、コミット `8cef03566d80f19c1c9004076279ffc054aec829`。開発ブランチは `development/next` です。
- 固定バックアップは `/Users/risakonishi/.codex/.chatgpt-projects/g-p-6a9d9eb11f508191a9b1e203ad99eafb/releases/pitakabe-v1.0.0`。その `release.json` と `ROLLBACK.md` を参照してください。
- 元のwallfitリポジトリと固定バックアップは読み取り専用です。元のソース、タグ、保存版、公開先を変更しないでください。
- このコピーの `origin` は削除済みです。元のリポジトリや公開先に再接続しないでください。
- 既存のソースを雛形として継承します。アプリの初期化ツールで上書きせず、構成、依存関係、ロックファイルを維持してください。

## 公開先

- ver.1.0の公開URLは `https://pitakabe.r5ni4.chatgpt.site` です。
- 次期版を正式版として既存Site `appgprj_6a9e4a066e3c8191b0a0c7b7733a02b5` に反映し、上記の旧URLを継続して使用します。
- `.openai/hosting.json` の `project_id` は正式版SiteのIDと一致させます。次期版専用Siteは作業用の別公開先として扱い、正式版の公開先にしません。
- 旧Siteの現在のアクセス設定は維持し、ユーザーから明示的な変更依頼がない限り公開範囲を変更しません。

## 継承する機能と方式

- 画像処理はブラウザ内で完結させます。外部の有料AI APIを導入しないでください。
- iPhone 12〜17系・Airの26機種サイズ、上下の余白、画像サイズ・上下位置、切り取り、背景色、時計／Dockのプレビュー、PNG保存を継承します。
- 保存中の設定変更に伴う古い結果の破棄、連続保存の防止、共有判定失敗時のダウンロードへの移行、スライダーのラベル・操作領域の修正を維持してください。
- `package.json` の `0.1.0` は雛形のメタデータであり、製品ver.1.0とは別です。

## 検証の扱い

- `QA_REPORT.md` のver.1.0検証履歴と既存テストを継承し、次期版で実施した検証を追記しています。最新の検証範囲はその追記を確認してください。
- コピー準備時にはアプリの検証を再実行していません。その後の次期版検証と区別し、過去の検証だけを次期版の検証結果として扱わないでください。
- ネイティブCanvasとサーバーレンダリングの検証は、実ブラウザやiPhone実機での検証の代わりにはなりません。
- ver.1.0で未確認だったブラウザ操作を一括して解決済みとしないでください。次期版で個別に確認できた事項だけを `QA_REPORT.md` に記録し、未実施のブラウザ／操作／実機確認は残してください。
- iPhoneの写真への保存、OSの壁紙設定、時計拡大、ウィジェット・通知との重なりも未確認です。具体的な検証なしに解決済みとしないでください。

## 今後の作業

- 今回の対象は、検索＋Dockを避けるプレビューと配置、シリーズ→機種の2段階選択、無地・縦ストライプ・斜め線・星・水玉の固定プリセットです。
- 模様はCanvasで背景として描き、描画色は背景色から自動決定する1色だけとします。色・密度・サイズ・角度の調整UIは追加しません。
- 製品名は「次期版」とし、正式な番号や別ブランド名はユーザーが指定するまで付けません。
- 初期のサンプル画像、背景色パレット、faviconの色は維持し、ユーザーの明示的な依頼なしに変更しません。
- 実機参考画像には個人的な本文が含まれます。公開資産、サンプル、テスト、Gitリポジトリへコピーしないでください。
- 追加要望が届くまでは、今回の3点以外の機能を推測して実装しないでください。
