# ぴた壁 — 検証記録

実施日: 2026-09-07

## 実施できた検証

- 機種サイズ・余白・クロップ・縮小・上下位置の50,544通りで、画像が指定した領域内に収まることを検証。
- アプリと同じ `renderWallpaper` 関数を、ブラウザとは独立したネイティブCanvasに接続。26機種 × 4種類、計104個のPNGを実際にエンコードし、再デコード。
- 入力は縦長PNG、上下を切り取る横長JPEG、透明部分のあるWebP、長いスクショを想定したPNG。出力PNGのヘッダー、機種別の幅・高さ、四辺の背景色・不透明度、画像中央の画素、上下の切り取り結果を確認。
- 余白内をサンプリングして背景色と一致することを確認。時計・DockなどのUIは描画関数に含まれない。
- PNGエンコード完了前に設定が変わった場合、古いPNGを破棄する回帰テスト。
- PNGエンコード失敗時のエラー、黒や紺など任意の暗い背景色への明るいガイドの選択を検証。
- 実際のページコンポーネントをReactのサーバーレンダラーでHTML化。6個のnative range inputが、それぞれ可視の日本語ラベルを参照することを確認。これはブラウザ操作検証ではない。

## 修正

1. 保存処理中に画像・機種・色などが変わったら、古い結果を表示せず再保存を案内。
2. 連続した保存処理を防ぐ同期ガードを追加。
3. 共有機能の対応判定が失敗しても、PNGダウンロードに進めるよう変更。
4. 6個のスライダーに読み上げ用ラベルを関連付け、操作領域の高さを44pxに拡大。ブラウザでの実際のタッチ判定は未確認。
5. 任意の背景色の明るさから、時計・ホームバーなどのガイド色を選択。

## 未確認・制約

- 公開版の既存タブをブラウザツールで開く操作は2回とも拒否された。理由は「管理者ポリシーを検証できず、ブラウザのセキュリティチェックを完了できない」。アクセス制御を迂回せず、画面クリック・リサイズ・スクリーンショット取得・ブラウザダウンロード検証は実施していない。
- このネイティブCanvas検証は、SafariやChromiumのCanvas実装・画像デコーダー・ダウンロード動作を保証しない。
- 画像入力ダイアログ、貼り付け、ドラッグ＆ドロップ、連続画像読み込み、キーボード／タッチ操作、モバイルレイアウト、共有メニューの実操作は未確認。
- モバイルでは調整欄とプレビューの間のスクロールが多いため、画面操作を再開できたら確認・改善を検討。
- iPhoneの写真への保存、OSの壁紙設定、時計の拡大、ウィジェット・通知との実際の重なりは実機確認が必要。
- 開発中に記録されたHMR更新時のエラーについて、公開版への影響はブラウザで未確認。
- WebMCPの実ブラウザ登録／操作は未検証。

## 再実行

標準検証: `node --test tests/wallpaper.test.mjs tests/export.test.mjs tests/accessibility-ssr.test.mjs`

実PNG検証: `QA_CANVAS_MODULE=/absolute/path/to/@napi-rs/canvas node --test tests/png-output.test.mjs`

型検証: `npx tsc --noEmit`

生成した検証用PNGの一部はGit対象外の `work/qa/` に保持。ユーザー画像は使用していない。
