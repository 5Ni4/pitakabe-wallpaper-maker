# ぴた壁 — 検証記録

実施日: 2026-09-07

## 実施できた検証

- 機種サイズ・余白・クロップ・縮小・上下位置の50,544通りで、画像が指定した領域内に収まることを検証。
- アプリと同じ `renderWallpaper` 関数を、ブラウザとは独立したネイティブCanvasに接続。26機種 × 4種類、計104個のPNGを実際にエンコードし、再デコード。
- 入力は縦長PNG、上下を切り取る横長JPEG、透明部分のあるWebP、長いスクショを想定したPNG。出力PNGのヘッダー、機種別の幅・高さ、四辺の背景色・不透明度、画像中央の画素、上下の切り取り結果を確認。
- 余白内をサンプリングして背景色と一致することを確認。時計・DockなどのUIは描画関数に含まれない。
- PNGエンコード完了前に設定が変わった場合、古いPNGを破棄する回帰テスト。
- PNGエンコード失敗時のエラー、黒や紺など任意の暗い背景色への明るいガイドの選択を検証。
- 実際のページコンポーネントをReactのサーバーレンダラーでHTML化。6個のnative range inputが、それぞれ可視の日本語ラベルを参照することを確認。これはブラウザ操作検証ではない。

## 修正

1. 保存処理中に画像・機種・色などが変わったら、古い結果を表示せず再保存を案内。
2. 連続した保存処理を防ぐ同期ガードを追加。
3. 共有機能の対応判定が失敗しても、PNGダウンロードに進めるよう変更。
4. 6個のスライダーに読み上げ用ラベルを関連付け、操作領域の高さを44pxに拡大。ブラウザでの実際のタッチ判定は未確認。
5. 任意の背景色の明るさから、時計・ホームバーなどのガイド色を選択。

## 未確認・制約

- 公開版の既存タブをブラウザツールで開く操作は2回とも拒否された。理由は「管理者ポリシーを検証できず、ブラウザのセキュリティチェックを完了できない」。アクセス制御を迂回せず、画面クリック・リサイズ・スクリーンショット取得・ブラウザダウンロード検証は実施していない。
- このネイティブCanvas検証は、SafariやChromiumのCanvas実装・画像デコーダー・ダウンロード動作を保証しない。
- 画像入力ダイアログ、貼り付け、ドラッグ＆ドロップ、連続画像読み込み、キーボード／タッチ操作、モバイルレイアウト、共有メニューの実操作は未確認。
- モバイルでは調整欄とプレビューの間のスクロールが多いため、画面操作を再開できたら確認・改善を検討。
- iPhoneの写真への保存、OSの壁紙設定、時計の拡大、ウィジェット・通知との実際の重なりは実機確認が必要。
- 開発中に記録されたHMR更新時のエラーについて、公開版への影響はブラウザで未確認。
- WebMCPの実ブラウザ登録／操作は未検証。

## 再実行

標準検証: `node --test tests/wallpaper.test.mjs tests/export.test.mjs tests/accessibility-ssr.test.mjs`

実PNG検証: `QA_CANVAS_MODULE=/absolute/path/to/@napi-rs/canvas node --test tests/png-output.test.mjs`

型検証: `npx tsc --noEmit`

生成した検証用PNGの一部はGit対象外の `work/qa/` に保持。ユーザー画像は使用していない。

---

## 次期版の追加検証（2026-09-08）

上記はver.1.0の履歴です。以下は独立コピーでの次期版検証であり、ver.1.0の未確認事項を一括して解決済みにするものではありません。

### 実装した3点

- 初期の下余白とホーム用おまかせを24%に変更。ホームの検索（上端80%・高さ4%）、Dock（上端86%・高さ12%）、ホームインジケーターをプレビューのみに表示。ロック／ホームのタブ切り替えは手動調整値を変えません。
- シリーズ→機種の2段階選択。17／16／15／14／13／12／Airの各グループは4／5／4／4／4／4／1機種で、既存26機種のID・出力ピクセルサイズを維持。同じmini／Pro／Pro Max／e等が新シリーズにあれば引き継ぎ、なければ基本モデルを選択。
- 無地・縦ストライプ・斜め線・星・水玉。背景色から自動で1色を決めて背景に描画し、元画像を最後に合成。プレビューと保存は同じ `renderWallpaper`／`renderBackground` を使用。模様の追加調整UIや外部AI APIは追加していません。

### 自動検証

- 標準・追加回帰テスト14件が成功。50,544通りの配置計算、保存の古い結果の破棄／エラー、暗色背景のガイド、6個のスライダーラベルを再確認。
- 全26機種の固定サイズとシリーズの網羅性、系列変更時のモデル引き継ぎ、初期値／おまかせの画像下端が検索より少なくとも3%上に収まることを確認。
- 既存の104個に、26機種×5模様×明色／任意の暗色の260個を追加し、計364個の実PNGをエンコード・デコード。すべて成功。
- 出力寸法、不透明度、単一描画色と背景色の範囲、元画像の不透明部分を模様で覆わないこと、下部余白に検索／Dock等を焼き込まないことを検証。PNG読み戻しの全画素が描画直後のCanvasと一致。
- 縦線と斜線の区別、模様の再現性、無効な模様や未依頼の色・密度・角度設定の拒否も検証。

### 実ブラウザで確認できた事項

- 今回は正規のブラウザ安全確認を通過。Codex内蔵ブラウザでlocalhostの次期版を操作。個人情報を含まない生成サンプルPNGをファイル選択から読み込み、機種／色／模様／余白を変更し、完成画像を表示。
- 12シリーズ内の4機種、17シリーズ内の4機種を実際に開いて選択。12 mini、17e、16e、Airの表示・サイズ更新を確認。17e→16シリーズで16eを引き継がない挙動を検出し、依存する選択欄の再マウントと最新設定を使う更新に修正した後、17e→16eを再確認。
- 393×852と320×740のビューポートで選択・模様・保存ボタン等を確認。両幅とも文書幅がビューポート幅と一致し、横方向のはみ出しなし。5個の模様ボタンは高さ44px。下余白スライダーをキーボードで24%→25%に変更できた。
- 320px幅で検索ガイドの上端80%、Dock上端約86%をDOMの表示位置から確認。暗色＋水玉の保存画面では時計・検索・Dock・ガイドのない完成画像を確認。
- 「PNGをダウンロード」を押した後、ブラウザツールのdownloadイベント通知はタイムアウトしたが、実際のダウンロードフォルダへの新規保存を確認。保存した12 mini用PNGを別途読み戻し、1080×2340・背景色#252b38を確認。検証用コピーをGit対象外の `work/qa/browser-downloaded-mini-dots.png` に保持。
- WebMCPの実登録、設定読み取り、Air／縦ストライプ／下余白24%への設定変更とUIへの反映を確認。この検証時点でブラウザのerror／warnログは0件。

### 残る確認

- 上記のビューポート検証はiPhone実機のタッチ検証ではありません。iPhoneの写真への保存、OSの壁紙設定、時計拡大、ウィジェット／通知との重なり、iOSや機種ごとの検索位置差は実機確認が必要です。
- Safari／Chromiumそれぞれを網羅した比較、貼り付け・ドラッグ＆ドロップ・連続読み込み、共有シートの実操作は今回未実施です。
- 従来のモバイル表示では調整欄とプレビューの間に縦スクロールが必要です。今回の対象外のレイアウト変更はしていません。
- ver.1.0のHMRエラーの公開版への影響を、今回のローカル検証だけで解決済みとはしていません。

### 追加テストの再実行

標準・追加回帰: `node --test tests/wallpaper.test.mjs tests/export.test.mjs tests/accessibility-ssr.test.mjs tests/next-version.test.mjs`

実PNG: `QA_CANVAS_MODULE=/absolute/path/to/@napi-rs/canvas node --test tests/png-output.test.mjs tests/pattern-png.test.mjs`

型検証: `npx tsc --noEmit --incremental false`
